import type React from "react"
import "./globals.css"
import type { Metadata } from "next"
import { Inter } from "next/font/google"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "Horn Business Solution - Empowering Businesses with Smart Solutions",
  description:
    "Your trusted partner in business management solutions. We provide innovative and efficient systems to help businesses streamline operations, enhance productivity, and achieve success.",
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}

